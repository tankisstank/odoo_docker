<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_order_form_inherit_trade_in_combined_picking" model="ir.ui.view">
            <field name="name">sale.order.form.inherit.trade_in.combined</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
                <!-- Arrow Button Logic Removed -->
            <field name="arch" type="xml">
                <!-- Add Pawn Button to Header -->
                <xpath expr="//header" position="inside">
                    <!-- Ensure custom_state is loaded for attrs evaluation -->
                    <field name="custom_state" invisible="1"/>
                    <field name="settled_to_order_id" invisible="1"/>

                    <button name="action_super_cancel" string="Hủy đơn hàng" type="object" class="btn-danger"
                        confirm="CẢNH BÁO: Bạn đang thực hiện HỦY ĐƠN HÀNG. Hệ thống sẽ tự động hoàn tác các phiếu kho và hóa đơn liên quan. Bạn có chắc chắn muốn tiếp tục?"
                        attrs="{'invisible': [('state', 'not in', ['sale', 'done'])]}"/>
                </xpath>

                <xpath expr="//sheet/div[@name='button_box']" position="after">
                     <!-- 1. Cancelled -->
                    <widget name="web_ribbon" title="ĐÃ HỦY" bg_color="bg-danger" attrs="{'invisible': [('state', '!=', 'cancel')]}"/>
                    
                    <!-- 2. Completed (Normal) - Only show if DONE and NOT TRANSFERRED -->
                    <widget name="web_ribbon" title="ĐÃ HOÀN TẤT" bg_color="bg-success" attrs="{'invisible': ['|', ('state', '!=', 'done'), ('settled_to_order_id', '!=', False)]}"/>
                    
                    <!-- 3. Settled/Transferred - Show if TRANSFERRED -->
                    <widget name="web_ribbon" title="ĐÃ CHUYỂN ĐƠN" bg_color="bg-info" attrs="{'invisible': [('settled_to_order_id', '=', False)]}"/>
                </xpath>

                <xpath expr="//field[@name='partner_id']" position="before">
                     <!-- Prominent Link to New Order -->
                     <div class="alert alert-info" role="alert" style="margin-bottom: 5px;" attrs="{'invisible': [('settled_to_order_id', '=', False)]}">
                        <strong>ĐƠN HÀNG ĐÃ ĐƯỢC CHUYỂN SANG: </strong>
                        <field name="settled_to_order_id" readonly="1" options="{'no_open': False}" class="oe_inline"/>
                     </div>
                </xpath>

                <xpath expr="//field[@name='partner_id']" position="attributes">
                     <attribute name="attrs">{'readonly': ['|', '|', ('state', 'in', ['done', 'cancel']), ('custom_state', '=', 'invoiced'), ('custom_state', '=', 'done_delivery')]}</attribute>
                </xpath>
                
                <xpath expr="//field[@name='partner_id']" position="after">
                    <field name="legacy_type" widget="badge" decoration-info="legacy_type == 'nhap'" decoration-warning="legacy_type == 'xuat'" decoration-success="legacy_type == 'hdkb'" decoration-primary="legacy_type == 'hdkm'"/>
                    <field name="legacy_transaction_status" widget="badge" decoration-danger="legacy_transaction_status == 'khach_no'" decoration-warning="legacy_transaction_status == 'khach_gui'" decoration-success="legacy_transaction_status == 'can_bang'"/>
                </xpath>

                <xpath expr="//field[@name='order_line']" position="attributes">
                     <attribute name="attrs">{'readonly': ['|', '|', ('state', 'in', ['done', 'cancel']), ('custom_state', '=', 'invoiced'), ('custom_state', '=', 'done_delivery')]}</attribute>
                </xpath>

                <xpath expr="//button[@name='action_view_delivery']" position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath>

                <xpath expr="//button[@name='action_view_delivery']" position="after">
                    <button type="object"
                        name="action_view_combined_transfers"
                        class="oe_stat_button"
                        icon="fa-exchange"
                        attrs="{'invisible': [('delivery_count', '=', 0)]}">
                        <field name="delivery_count" widget="statinfo" string="Giao Nhận"/>
                    </button>
                </xpath>
                
                <xpath expr="//field[@name='payment_term_id']" position="after">
                    <field name="auto_balance_money"/>
                </xpath>
                
                



                <xpath expr="//notebook" position="after">
                    <separator string="Đơn hàng chưa hoàn thành" style="background-color: lightgrey;"   />
                    <field name="pending_order_ids" mode="tree" readonly="1">
                        <tree>
                            <field name="name" string="Mã GD"/>
                            <field name="date_order" widget="datetime" string="Thời gian"/>
                            <field name="note" string="Ghi chú" optional="show"/>
                            <field name="summary_goods_in" string="Hàng Nhập" optional="show"/>
                            <field name="summary_goods_out" string="Hàng Xuất" optional="show"/>
                            <field name="money_total_in" string="Tiền Nhập" optional="show" decoration-danger="1" sum="Tổng Tiền Nhập"/>
                            <field name="money_total_out" string="Tiền Xuất" optional="show" decoration-danger="1" sum="Tổng Tiền Xuất"/>
                            <field name="legacy_type" string="Loại" widget="badge" 
                                decoration-info="legacy_type == 'nhap'" 
                                decoration-warning="legacy_type == 'xuat'" 
                                decoration-success="legacy_type == 'hdkb'" 
                                decoration-primary="legacy_type == 'hdkm'" optional="show"/>
                            <field name="legacy_transaction_status" string="Nợ" widget="badge" 
                                decoration-danger="legacy_transaction_status == 'khach_no'" 
                                decoration-warning="legacy_transaction_status == 'khach_gui'" 
                                decoration-success="legacy_transaction_status == 'can_bang'" optional="show"/>
                            
                            <field name="custom_state" string="Tình trạng"
                                widget="badge" 
                                decoration-success="custom_state == 'done_delivery'" 
                                decoration-info="custom_state == 'partial'" 
                                decoration-muted="custom_state == 'invoiced'"/>
                                
                            <button name="action_settle_debt" type="object" icon="fa-money" 
                                string="Thanh toán" help="Cấn trừ vào đơn hàng hiện tại"
                                context="{'target_order_id': parent.id}"/>
                            <field name="state" invisible="1"/>
                        </tree>
                    </field>
                    
                    <!--
                    <separator string="Đơn Cầm cố / Vay mượn đang hiệu lực" style="background-color: #ffeba1; margin-top: 20px;"/>
                    <field name="pending_pawn_ids" mode="tree" readonly="1">
                        <tree>
                            <field name="name"/>
                            <field name="date_order"/>
                            <field name="amount_loan" sum="Tổng vay"/>
                            <field name="interest_rate"/>
                             <field name="state" 
                                widget="badge" 
                                decoration-info="state == 'draft'" 
                                decoration-warning="state == 'confirmed'"/>
                        </tree>
                    </field>
                    -->
                </xpath>

            </field>
        </record> 

        <record id="view_order_tree_trade_in_custom" model="ir.ui.view">
            <field name="name">sale.order.tree.trade.in.custom</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_tree"/>
            <field name="arch" type="xml">
                <tree position="attributes">
                    <attribute name="decoration-warning">invoice_status == 'invoiced'</attribute>
                </tree>
                <field name="name" position="attributes">
                    <attribute name="string">Mã GD</attribute>
                </field>
                <field name="partner_id" position="attributes">
                    <attribute name="string">Bạn hàng</attribute>
                </field>
                <field name="partner_id" position="after">
                    <field name="note" string="Ghi chú" optional="show"/>
                    <field name="summary_goods_in" string="Hàng Nhập" optional="show"/>
                    <field name="summary_goods_out" string="Hàng Xuất" optional="show"/>
                    <field name="money_total_in" string="Tiền Nhập" optional="show" decoration-danger="1" sum="Tổng Tiền Nhập"/>
                    <field name="money_total_out" string="Tiền Xuất" optional="show" decoration-danger="1" sum="Tổng Tiền Xuất"/>
                    <field name="legacy_type" string="Loại (Legacy)" widget="badge" decoration-info="legacy_type == 'nhap'" decoration-warning="legacy_type == 'xuat'" decoration-success="legacy_type == 'hdkb'" decoration-primary="legacy_type == 'hdkm'" optional="show"/>
                    <field name="legacy_transaction_status" string="Nợ (Legacy)" widget="badge" decoration-danger="legacy_transaction_status == 'khach_no'" decoration-warning="legacy_transaction_status == 'khach_gui'" decoration-success="legacy_transaction_status == 'can_bang'" optional="show"/>
                </field>
                <field name="date_order" position="attributes">
                    <attribute name="string">Thời gian</attribute>
                    <attribute name="widget">datetime</attribute>
                </field>
                <field name="date_order" position="after">
                    <field name="create_date" string="Thời gian lưu" widget="datetime" optional="show"/>
                </field>
                <field name="state" position="replace">
                    <field name="custom_state" string="Tình trạng" 
                        decoration-success="custom_state == 'done_delivery'" 
                        decoration-info="custom_state == 'partial'" 
                        decoration-muted="custom_state == 'invoiced'"
                        widget="badge"/>
                    <field name="state" invisible="1"/>
                </field>
                
                <!-- Hide unnecessary columns to match the simple view -->
                <field name="user_id" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
                <field name="team_id" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
                <field name="company_id" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
                <field name="amount_total" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
                <field name="invoice_status" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
                <field name="activity_ids" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
                 <field name="amount_tax" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
                 <field name="expected_date" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
            </field>
        </record>


        <record id="view_quotation_tree_trade_in_custom" model="ir.ui.view">
            <field name="name">sale.quotation.tree.trade.in.custom</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_quotation_tree"/>
            <field name="arch" type="xml">
                <tree position="attributes">
                    <attribute name="decoration-warning">invoice_status == 'invoiced'</attribute>
                </tree>
                <field name="name" position="attributes">
                    <attribute name="string">Mã GD</attribute>
                </field>
                <field name="partner_id" position="attributes">
                    <attribute name="string">Bạn hàng</attribute>
                </field>
                <field name="partner_id" position="after">
                    <field name="note" string="Ghi chú" optional="show"/>
                    <field name="summary_goods_in" string="Hàng Nhập" optional="show"/>
                    <field name="summary_goods_out" string="Hàng Xuất" optional="show"/>
                    <field name="money_total_in" string="Tiền Nhập" optional="show" decoration-danger="1" sum="Tổng Tiền Nhập"/>
                    <field name="money_total_out" string="Tiền Xuất" optional="show" decoration-danger="1" sum="Tổng Tiền Xuất"/>
                    <field name="legacy_type" string="Loại (Legacy)" widget="badge" decoration-info="legacy_type == 'nhap'" decoration-warning="legacy_type == 'xuat'" decoration-success="legacy_type == 'hdkb'" decoration-primary="legacy_type == 'hdkm'" optional="show"/>
                    <field name="legacy_transaction_status" string="Nợ (Legacy)" widget="badge" decoration-danger="legacy_transaction_status == 'khach_no'" decoration-warning="legacy_transaction_status == 'khach_gui'" decoration-success="legacy_transaction_status == 'can_bang'" optional="show"/>
                </field>
                <field name="create_date" position="attributes">
                     <attribute name="string">Thời gian tạo</attribute>
                     <attribute name="optional">show</attribute>
                     <attribute name="widget">datetime</attribute>
                </field>
                 <field name="state" position="replace">
                    <field name="custom_state" string="Tình trạng" 
                        decoration-success="custom_state == 'done_delivery'" 
                        decoration-info="custom_state == 'partial'" 
                        decoration-muted="custom_state == 'invoiced'"
                        widget="badge"/>
                    <field name="state" invisible="1"/>
                </field>
                
                 <!-- Optimize columns for Quotation View -->
                <field name="user_id" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
                <field name="team_id" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
                <field name="company_id" position="attributes">
                     <attribute name="optional">hide</attribute>
                </field>
                 <field name="amount_total" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
                 <field name="activity_ids" position="attributes">
                    <attribute name="optional">hide</attribute>
                </field>
            </field>
        </record>
    </data>
</odoo>